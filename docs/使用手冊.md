# 自留保費統計表報表轉換系統 — 使用手冊

> **版本**：1.0.0  
> **技術棧**：Java 17 / Spring Boot 3.4.1 / Apache POI 5.3.0 / Gradle 8.12

---

## 目錄

1. [系統概述](#1-系統概述)
2. [系統架構圖](#2-系統架構圖)
3. [快速開始](#3-快速開始)
4. [安裝與部署](#4-安裝與部署)
5. [設定說明](#5-設定說明)
6. [使用方式](#6-使用方式)
7. [輸入輸出規格](#7-輸入輸出規格)
8. [處理流程圖](#8-處理流程圖)
9. [模組架構與職責](#9-模組架構與職責)
10. [資料流圖](#10-資料流圖)
11. [險種歸類規則](#11-險種歸類規則)
12. [模板寫入邏輯](#12-模板寫入邏輯)
13. [錯誤處理與日誌](#13-錯誤處理與日誌)
14. [常見問題 (FAQ)](#14-常見問題-faq)

---

## 1. 系統概述

本系統將多個產險公司的「自留保費統計表」Excel 來源檔，自動彙整填入輸出模板，產生季度報表。

### 功能摘要

| 功能 | 說明 |
|------|------|
| 多檔案讀取 | 同時匯入多家公司的來源 Excel |
| 自動險種歸類 | 33 種險種自動歸類為 16 大類 |
| 季度判斷 | 依檔名自動判定季度 (Q1-Q4) |
| 公式保留 | 輸出報表完整保留 Excel 公式 |
| 動態公司顯示 | 僅顯示有資料的公司，隱藏空列 |
| 去年同期比較 | 自動讀取去年報表填入對照欄 |
| 容器化部署 | 支援 Docker 一鍵執行 |

---

## 2. 系統架構圖

### 2.1 整體架構

```mermaid
graph TB
    subgraph Input["📂 輸入"]
        SRC1["29_115(01-03)_自留保費統計表.xlsx"]
        SRC2["05_115(01-03)_自留保費統計表.xlsx"]
        SRC3["01_115(01-03)_自留保費統計表.xlsx"]
        TMPL["模板.xlsx"]
        LAST["去年報表.xlsx"]
    end

    subgraph App["⚙️ 報表轉換系統 (Spring Boot)"]
        RUNNER["ReportGeneratorRunner<br/>CLI 進入點"]
        GEN["ReportGeneratorService<br/>流程串接"]
        FP["FilenameParserService<br/>檔名解析"]
        ER["ExcelReaderService<br/>Excel 讀取"]
        DT["DataTransformerService<br/>資料轉換"]
        TW["TemplateWriterService<br/>模板寫入"]
        LY["LastYearReaderService<br/>去年資料"]
        VS["ValidationService<br/>驗證"]
    end

    subgraph Output["📄 輸出"]
        OUT["115年產險業務(Q1季自留)保費統計表.xlsx"]
        LOG["應用程式日誌"]
    end

    SRC1 --> FP
    SRC2 --> FP
    SRC3 --> FP
    FP --> GEN
    SRC1 --> ER
    SRC2 --> ER
    SRC3 --> ER
    ER --> GEN
    GEN --> VS
    GEN --> DT
    LAST --> LY
    LY --> GEN
    GEN --> TW
    TMPL --> TW
    TW --> OUT
    GEN --> LOG
    RUNNER --> GEN
```

### 2.2 技術架構

```mermaid
graph LR
    subgraph Runtime["執行環境"]
        JDK["Java 17<br/>(Corretto/Temurin)"]
        SB["Spring Boot 3.4.1"]
        POI["Apache POI 5.3.0"]
    end

    subgraph Build["建置工具"]
        GRD["Gradle 8.12"]
        JU["JUnit 5"]
    end

    subgraph Deploy["部署方式"]
        JAR["Fat JAR<br/>本機直接執行"]
        DCK["Docker Container<br/>容器化部署"]
    end

    GRD --> JAR
    GRD --> DCK
    JDK --> SB
    SB --> POI
```

---

## 3. 快速開始

### 前置需求

- **Java 17** (JDK)
- **Docker** (選用，容器化部署)

### 30 秒快速體驗

```bash
# 1. 建立目錄結構
mkdir input output templates lastyear

# 2. 放入模板
cp docs/115年產險業務(Q1季自留)保費統計表.xlsx templates/template.xlsx

# 3. 放入來源檔案
cp docs/29_115(01-03)_自留保費統計表.xlsx input/

# 4. 執行
.\gradlew.bat bootRun

# 5. 查看輸出
ls output/
# → 115年產險業務(Q1季自留)保費統計表.xlsx
```

---

## 4. 安裝與部署

### 4.1 本機執行 (開發/除錯)

```bash
# 建置
.\gradlew.bat build

# 執行（使用 application.yml 預設路徑）
.\gradlew.bat bootRun

# 執行（自訂路徑，以命令列參數覆蓋）
.\gradlew.bat bootRun --args="./input ./templates/template.xlsx ./output ./lastyear"

# 直接使用 JAR
.\gradlew.bat bootJar
java -jar build/libs/retained-premium-report-transformer-0.0.1-SNAPSHOT.jar
```

### 4.2 Docker 部署

```bash
# 建置映像檔
docker-compose build

# 執行
docker-compose up

# 背景執行
docker-compose up -d
```

#### Docker 目錄掛載

```
./input/       → /data/input       (來源檔案)
./output/      → /data/output      (輸出目錄)
./templates/   → /data/templates   (模板檔案)
./lastyear/    → /data/lastyear    (去年報表)
```

### 4.3 目錄結構

```
project-root/
├── input/                     # 放入來源 xlsx 檔案
│   ├── 29_115(01-03)_自留保費統計表.xlsx
│   ├── 05_115(01-03)_自留保費統計表.xlsx
│   └── ...
├── output/                    # 系統自動產出報表
│   └── 115年產險業務(Q1季自留)保費統計表.xlsx
├── templates/
│   └── template.xlsx          # 輸出模板（必須）
├── lastyear/                  # 去年報表（選用）
│   └── 114年產險業務(Q1季自留)保費統計表.xlsx
└── (程式碼目錄)
```

---

## 5. 設定說明

### 5.1 application.yml

```yaml
app:
  input-dir: ./input                           # 來源檔案目錄
  output-dir: ./output                         # 輸出目錄
  template-path: ./templates/template.xlsx     # 模板路徑
  last-year-dir: ./lastyear                    # 去年報表目錄

logging:
  level:
    com.example.retainedpremium: INFO          # 日誌等級
```

### 5.2 覆蓋優先順序

```
命令列參數 > 環境變數 > application.yml
```

| 參數位置 | application.yml | 環境變數 | CLI 參數 |
|----------|-----------------|----------|----------|
| 輸入目錄 | `app.input-dir` | `APP_INPUT_DIR` | 第 1 個參數 |
| 模板路徑 | `app.template-path` | `APP_TEMPLATE_PATH` | 第 2 個參數 |
| 輸出目錄 | `app.output-dir` | `APP_OUTPUT_DIR` | 第 3 個參數 |
| 去年目錄 | `app.last-year-dir` | `APP_LAST_YEAR_DIR` | 第 4 個參數 |

---

## 6. 使用方式

### 6.1 基本用法

1. 將所有來源 `.xlsx` 檔案放入 `input/` 目錄
2. 確認 `templates/template.xlsx` 存在
3. 執行程式
4. 從 `output/` 取得產出報表

### 6.2 多公司匯入

放入多個來源檔案，系統會自動合併：

```
input/
├── 01_115(01-03)_自留保費統計表.xlsx    # 台產
├── 05_115(01-03)_自留保費統計表.xlsx    # 富邦
├── 29_115(01-03)_自留保費統計表.xlsx    # 美國國際
└── 32_115(01-03)_自留保費統計表.xlsx    # 美商安達
```

輸出：`115年產險業務(Q1季自留)保費統計表.xlsx`（僅顯示這 4 家公司，其餘隱藏）

### 6.3 多季度匯入

同時放入不同季度的檔案，系統會自動填入對應區塊：

```
input/
├── 29_115(01-03)_自留保費統計表.xlsx    # Q1
├── 29_115(01-06)_自留保費統計表.xlsx    # Q2
└── 05_115(01-03)_自留保費統計表.xlsx    # Q1
```

輸出：`115年產險業務(Q2季自留)保費統計表.xlsx`（檔名用最大季度）

### 6.4 去年同期對比

將去年報表放入 `lastyear/` 目錄：

```
lastyear/
└── 114年產險業務(Q1季自留)保費統計表.xlsx
```

系統會自動讀取 Sheet2 的 T 欄值填入 U 欄（去年同期合計），並計算成長率。

> ⚠️ 若去年檔案不存在，U 欄留空，日誌記錄 WARN，不影響其他功能。

---

## 7. 輸入輸出規格

### 7.1 來源檔名格式

```
{公司別}_{年度}({起始月}-{結束月})_自留保費統計表.xlsx
```

```mermaid
graph LR
    FN["29_115(01-03)_自留保費統計表.xlsx"]
    FN --> C["公司別: 29"]
    FN --> Y["年度: 115"]
    FN --> SM["起始月: 01"]
    FN --> EM["結束月: 03"]
    FN --> Q["季度: Q1"]
```

#### 季度判定規則（累計制）

| 起始月 | 結束月 | 季度 | 月份標記 |
|--------|--------|------|----------|
| 01 | 03 | Q1 | `1-1Q(1-3)` |
| 01 | 06 | Q2 | `1-2Q(1-6)` |
| 01 | 09 | Q3 | `1-3Q(1-9)` |
| 01 | 12 | Q4 | `1-4Q(1-12)` |

### 7.2 來源檔 Excel 結構

```mermaid
graph TD
    subgraph Source["來源 Excel 結構"]
        R1["Row 1: 標題"]
        R4["Row 4: B4=公司代號, C4=公司名稱"]
        R5["Row 5: 欄位標題"]
        R7["Row 7~39: 33 種險種資料"]
        R40["Row 40: 合計列"]
    end

    subgraph Cols["欄位"]
        A["A: 險種代號"]
        B["B: 險種名稱"]
        C["C: 簽單保費收入"]
        D["D: 分進再保收入"]
        E["E: 分出再保支出"]
        F["F: 自留保費 ✨<br/>(程式讀取此欄)"]
        G["G: 自留比率"]
    end
```

### 7.3 輸出檔名格式

```
{年度}年產險業務(Q{最大季度}季自留)保費統計表.xlsx
```

### 7.4 輸出 Excel 結構

| Sheet | 名稱 | 用途 |
|-------|------|------|
| Sheet 1 | `{年度}自留(季)` | 各公司 × 33 險種明細 |
| Sheet 2 | `{年度}自留總累` | 各公司 × 16 分類彙總 + 去年對比 |
| Sheet 3 | `歸屬` | 險種分類對照表（不修改） |

---

## 8. 處理流程圖

### 8.1 主流程

```mermaid
flowchart TD
    START([開始]) --> SCAN["Step 1: 掃描輸入目錄<br/>找出所有 .xlsx 檔案"]
    SCAN --> PARSE["Step 2: 檔名解析<br/>提取公司別/年度/季度"]
    PARSE --> PRE_VAL{"Step 3: 前置驗證<br/>年度一致？重複公司？"}

    PRE_VAL -->|❌ 失敗| ABORT1([中止並記錄 ERROR])
    PRE_VAL -->|✅ 通過| READ["Step 4: 讀取來源 Excel<br/>提取各險種自留保費"]

    READ --> READ_OK{"讀取成功？"}
    READ_OK -->|❌ 任一失敗| ABORT2([中止並記錄 ERROR])
    READ_OK -->|✅ 全部成功| DATA_VAL["Step 5: 資料驗證<br/>數值/空值/異常檢查"]

    DATA_VAL --> DATA_OK{"驗證通過？"}
    DATA_OK -->|❌ 失敗| ABORT3([中止並記錄 ERROR])
    DATA_OK -->|✅ 通過| GROUP["Step 6: 按季度分組<br/>+ 險種歸類 (33→16)"]

    GROUP --> LASTYEAR["Step 7: 讀取去年報表<br/>（無檔案則跳過）"]
    LASTYEAR --> TMPL_CHK{"Step 8: 模板存在？"}

    TMPL_CHK -->|❌ 不存在| ABORT4([中止並記錄 ERROR])
    TMPL_CHK -->|✅ 存在| WRITE["Step 9-10: 寫入模板<br/>產出報表"]

    WRITE --> DONE([✅ 完成])

    style ABORT1 fill:#f44,color:#fff
    style ABORT2 fill:#f44,color:#fff
    style ABORT3 fill:#f44,color:#fff
    style ABORT4 fill:#f44,color:#fff
    style DONE fill:#4a4,color:#fff
```

### 8.2 模板寫入細節流程

```mermaid
flowchart TD
    OPEN["開啟模板 .xlsx<br/>(保留公式+格式)"] --> MAP["建立公司代號→列號映射<br/>(讀取 A 欄)"]

    MAP --> S1["寫入 Sheet1<br/>各公司 × 33 險種"]
    S1 --> S2["寫入 Sheet2<br/>各公司 × 16 分類"]
    S2 --> U["寫入 U 欄<br/>(去年同期數值)"]
    U --> HIDE["隱藏無資料公司列<br/>(setZeroHeight=true)"]
    HIDE --> TITLE["更新動態標題<br/>(年度/季度/月份)"]
    TITLE --> RENAME["重新命名 Sheet"]
    RENAME --> RECALC["設定公式重新計算"]
    RECALC --> SAVE["儲存輸出 .xlsx"]

    subgraph preserve["🔒 不觸碰的公式區域"]
        F1["Sheet1 AK 欄: =SUM(D:AJ)"]
        F2["Sheet1 小計列: =SUBTOTAL(9,...)"]
        F3["Sheet2 T 欄: =SUM(D:S)"]
        F4["Sheet2 V 欄: =IF(U=0,'',(T/U)-1)"]
        F5["Sheet2 小計/總計列"]
    end

    style preserve fill:#e8f4fd,stroke:#2196F3
```

---

## 9. 模組架構與職責

### 9.1 類別圖

```mermaid
classDiagram
    class ReportGeneratorRunner {
        -ReportGeneratorService reportGenerator
        -AppConfig appConfig
        +run(String... args)
    }

    class ReportGeneratorService {
        -FilenameParserService filenameParser
        -ExcelReaderService excelReader
        -DataTransformerService dataTransformer
        -TemplateWriterService templateWriter
        -LastYearReaderService lastYearReader
        -ValidationService validator
        +generate(inputDir, templatePath, outputDir, lastYearDir)
        -scanInputFiles(inputDir) List~File~
    }

    class FilenameParserService {
        -Pattern FILENAME_PATTERN
        +parse(filepath) Optional~FileInfo~
    }

    class ExcelReaderService {
        +readSourceFile(filePath) Optional~CompanyData~
        ~getCellStringValue(cell) String
        -evaluateNumericCell(cell, evaluator) double
    }

    class DataTransformerService {
        +groupByQuarter(fileInfos, companyDataMap) Map~Integer_QuarterData~
        +aggregateCategories(companyData) Map~Integer_Double~
    }

    class TemplateWriterService {
        -DataTransformerService dataTransformerService
        +writeReport(templatePath, outputPath, quarterDataMap, lastYearData, year, maxQuarter)
        -buildCompanyRowMap(sheet, dataStart, dataEnd) Map~String_Integer~
        -setCellNumericValue(cell, value)
    }

    class LastYearReaderService {
        +readLastYearData(currentYear, quarter, lastYearDir) Map~String_Double~
        -getCompanyCode(cell) String
        -evaluateNumericCell(cell, evaluator) double
    }

    class ValidationService {
        +validateFileInfos(fileInfos) ValidationResult
        +validateCompanyData(data, filename) ValidationResult
        +validateAll(fileInfos, companyDataMap) ValidationResult
    }

    class AppConfig {
        -String inputDir
        -String outputDir
        -String templatePath
        -String lastYearDir
    }

    class FileInfo {
        <<record>>
        +String companyCode
        +int year
        +int startMonth
        +int endMonth
        +int quarter
        +String filename
    }

    class CompanyData {
        <<record>>
        +String companyCode
        +String companyName
        +Map~String_Double~ retainedPremiums
    }

    class QuarterData {
        <<record>>
        +int year
        +int quarter
        +int startMonth
        +int endMonth
        +Map~String_CompanyData~ companies
    }

    class ValidationResult {
        -boolean passed
        -List~String~ errors
        -List~String~ warnings
        +addError(msg)
        +addWarning(msg)
        +merge(other)
    }

    ReportGeneratorRunner --> ReportGeneratorService
    ReportGeneratorRunner --> AppConfig
    ReportGeneratorService --> FilenameParserService
    ReportGeneratorService --> ExcelReaderService
    ReportGeneratorService --> DataTransformerService
    ReportGeneratorService --> TemplateWriterService
    ReportGeneratorService --> LastYearReaderService
    ReportGeneratorService --> ValidationService
    TemplateWriterService --> DataTransformerService
    FilenameParserService ..> FileInfo
    ExcelReaderService ..> CompanyData
    DataTransformerService ..> QuarterData
    ValidationService ..> ValidationResult
```

### 9.2 模組依賴關係

```mermaid
graph LR
    subgraph Phase1["Phase 1 (無依賴)"]
        IC["InsuranceConstants"]
        MOD["Models"]
    end

    subgraph Phase2["Phase 2"]
        FP["FilenameParser"]
        ER["ExcelReader"]
        VS["Validator"]
    end

    subgraph Phase3["Phase 3"]
        DT["DataTransformer"]
    end

    subgraph Phase4["Phase 4"]
        TW["TemplateWriter"]
        LY["LastYearReader"]
    end

    subgraph Phase5["Phase 5"]
        GEN["ReportGenerator"]
        RUN["Runner"]
    end

    IC --> FP
    IC --> ER
    IC --> VS
    IC --> DT
    IC --> TW
    IC --> LY
    ER --> DT
    DT --> TW
    LY --> TW
    FP --> GEN
    TW --> GEN
    VS --> GEN
    GEN --> RUN
```

---

## 10. 資料流圖

### 10.1 資料轉換全流程

```mermaid
flowchart LR
    subgraph Input
        F1["來源檔 A<br/>(公司 29)"]
        F2["來源檔 B<br/>(公司 05)"]
        F3["來源檔 C<br/>(公司 01)"]
    end

    subgraph Parse["解析"]
        FI1["FileInfo<br/>code=29, Q1"]
        FI2["FileInfo<br/>code=05, Q1"]
        FI3["FileInfo<br/>code=01, Q1"]
    end

    subgraph Read["讀取"]
        CD1["CompanyData<br/>33 險種保費"]
        CD2["CompanyData<br/>33 險種保費"]
        CD3["CompanyData<br/>33 險種保費"]
    end

    subgraph Transform["轉換"]
        QD["QuarterData<br/>Q1: 3 家公司"]
        AGG["聚合分類<br/>33 → 16 類"]
    end

    subgraph Write["寫入"]
        S1["Sheet1<br/>33 險種明細"]
        S2["Sheet2<br/>16 分類彙總"]
    end

    F1 --> FI1 --> CD1
    F2 --> FI2 --> CD2
    F3 --> FI3 --> CD3
    CD1 --> QD
    CD2 --> QD
    CD3 --> QD
    QD --> S1
    QD --> AGG --> S2
```

### 10.2 Sheet1 寫入映射

```mermaid
graph TB
    subgraph Source["來源檔 (公司 29)"]
        SF["F 欄 (自留保費)"]
        SC["代號: 0100=100萬, 0200=50萬, ..."]
    end

    subgraph Template["模板 Sheet1"]
        direction TB
        R1["Row 1: 險種代號 0100  0200  ...  9900"]
        R2["Row 2: 欄標題    D     E    ...  AJ    AK"]
        R3["Row 3: 公司 01"]
        R18["Row 18: 公司 29 ← 寫入此列"]
        R22["Row 22: 小計 (SUBTOTAL 公式)"]
    end

    SF -->|"0100 → D18"| R18
    SF -->|"0200 → E18"| R18
    SF -->|"9900 → AJ18"| R18
```

### 10.3 Sheet2 險種聚合映射

```mermaid
graph LR
    subgraph Codes33["33 險種 (來源)"]
        C01["0100 一年期住宅火險"]
        C02["0200 長期住宅火險"]
        C03["0300 一年期商業火險"]
        C04["0400 長期商業火險"]
        C05["0500~0800"]
        C09["0900"]
        CX["...其餘 27 種"]
    end

    subgraph Cat16["16 分類 (Sheet2)"]
        D["D: 火險"]
        E["E: 水險"]
        F["F: 航空"]
        MORE["G~S: 其餘 13 類"]
    end

    C01 -->|SUM| D
    C02 -->|SUM| D
    C03 -->|SUM| D
    C04 -->|SUM| D
    C05 -->|SUM| E
    C09 -->|直接| F
```

---

## 11. 險種歸類規則

### 33 → 16 歸類對照表

```mermaid
graph TD
    subgraph 火險["D: 火險"]
        I0100["0100 一年期住宅火險"]
        I0200["0200 長期住宅火險"]
        I0300["0300 一年期商業火險"]
        I0400["0400 長期商業火險"]
    end

    subgraph 水險["E: 水險"]
        I0500["0500 內陸運輸險"]
        I0600["0600 貨物運輸險"]
        I0700["0700 船體險"]
        I0800["0800 漁船險"]
    end

    subgraph 車險["G-K: 車險相關"]
        I1000["1000+1100 → G 車體損失"]
        I1200["1200+1300 → H 任意責任"]
        I1400["1400+1500 → I 強制車責"]
        I1600["1600 → J 強制機車"]
        I3200["3200 → K 二輪電動"]
    end
```

| Sheet2 欄 | 分類名稱 | 組成險種代號 |
|-----------|----------|-------------|
| D | 火險 | 0100 + 0200 + 0300 + 0400 |
| E | 水險 | 0500 + 0600 + 0700 + 0800 |
| F | 航空 | 0900 |
| G | 車體損失險 | 1000 + 1100 |
| H | 任意責任險 | 1200 + 1300 |
| I | 強制車責 (汽車) | 1400 + 1500 |
| J | 強制機車責任險 | 1600 |
| K | 強制二輪電動 | 3200 |
| L | 責任險 | 1700 + 1800 |
| M | 工程險 | 1900 |
| N | 信用保證 | 2100 + 2200 |
| O | 其他財產 | 2000 + 2300 + 2600 + 2700 |
| P | 傷害險 | 2400 |
| Q | 天災險 | 2500 + 2800 + 2900 |
| R | 健康險 | 3000 + 3100 |
| S | 國外分進 | 9900 |

---

## 12. 模板寫入邏輯

### 12.1 Sheet1 區塊佈局

```mermaid
graph TD
    subgraph Sheet1["Sheet1: {年度}自留(季)"]
        H["Row 1-2: 標題列"]
        Q1["Q1 區塊<br/>Row 3~21 (公司資料)<br/>Row 22 (SUBTOTAL 小計)"]
        Q2["Q2 區塊<br/>Row 23~41 (公司資料)<br/>Row 42 (SUBTOTAL 小計)"]
        Q3["Q3 區塊<br/>Row 43~61 (公司資料)<br/>Row 62 (SUBTOTAL 小計)"]
        Q4["Q4 區塊<br/>Row 63~81 (公司資料)<br/>Row 82 (SUBTOTAL 小計)"]
    end

    H --> Q1 --> Q2 --> Q3 --> Q4
```

### 12.2 Sheet2 區塊佈局

```mermaid
graph TD
    subgraph Sheet2["Sheet2: {年度}自留總累"]
        H2["Row 1: 動態標題<br/>Row 2-5: 欄位標題"]
        Q1S2["Q1 區塊<br/>Row 6~24 (公司資料)<br/>Row 25 (SUBTOTAL 小計)"]
        Q2S2["Q2 區塊<br/>Row 26~44 (公司資料)<br/>Row 45 (SUBTOTAL 小計)"]
        Q3S2["Q3 區塊<br/>Row 46~64 (公司資料)<br/>Row 65 (SUBTOTAL 小計)"]
        Q4S2["Q4 區塊<br/>Row 66~84 (公司資料)<br/>Row 85 (SUBTOTAL 小計)"]
        GT["Row 86: 總計 (SUBTOTAL)"]
        CT["Row 87: 筆數"]
    end

    H2 --> Q1S2 --> Q2S2 --> Q3S2 --> Q4S2 --> GT --> CT
```

### 12.3 公式保留策略

```mermaid
graph LR
    subgraph Write["✏️ 程式寫入"]
        W1["Sheet1 D~AJ 欄<br/>(33 險種值)"]
        W2["Sheet2 D~S 欄<br/>(16 分類值)"]
        W3["Sheet2 U 欄<br/>(去年同期)"]
    end

    subgraph Keep["🔒 保留公式"]
        K1["Sheet1 AK 欄<br/>=SUM(D:AJ)"]
        K2["所有小計列<br/>=SUBTOTAL(9,...)"]
        K3["Sheet2 T 欄<br/>=SUM(D:S)"]
        K4["Sheet2 V 欄<br/>=IF(U=0,'',(T/U)-1)"]
        K5["Sheet2 Row86<br/>=SUBTOTAL(9,全部)"]
    end

    style Write fill:#e8f5e9,stroke:#4CAF50
    style Keep fill:#fff3e0,stroke:#FF9800
```

### 12.4 動態公司隱藏機制

```mermaid
stateDiagram-v2
    [*] --> 讀取模板: 19 家預設公司

    讀取模板 --> 匹配來源: 比對來源檔公司代號

    state 匹配來源 {
        有資料 --> 寫入數值: 填入自留保費
        無資料 --> 隱藏列: setZeroHeight(true)
    }

    寫入數值 --> SUBTOTAL自動計算
    隱藏列 --> SUBTOTAL自動計算

    note right of 隱藏列
        SUBTOTAL(9,...) 會自動
        忽略隱藏列，確保
        小計/總計正確
    end note

    SUBTOTAL自動計算 --> [*]: 輸出報表
```

---

## 13. 錯誤處理與日誌

### 13.1 錯誤分類表

```mermaid
graph TD
    subgraph FATAL["🔴 中止流程"]
        E1["模板檔案不存在"]
        E2["同公司同季度重複"]
        E3["年度不一致"]
        E4["來源資料讀取失敗"]
        E5["報表寫入失敗"]
    end

    subgraph WARN["🟡 警告繼續"]
        W1["去年檔案不存在<br/>(U 欄留空)"]
        W2["負值保費<br/>(可能為調整)"]
        W3["險種數 ≠ 33"]
    end

    subgraph SKIP["🟠 跳過此檔"]
        S1["檔名格式不符"]
        S2["公司代號不在模板中"]
    end

    style FATAL fill:#ffebee,stroke:#f44336
    style WARN fill:#fff8e1,stroke:#ffc107
    style SKIP fill:#fff3e0,stroke:#ff9800
```

### 13.2 日誌等級對照

| 情境 | Log 等級 | 行為 |
|------|----------|------|
| 檔名格式不符 | `ERROR` | 跳過此檔，繼續 |
| 來源 Excel 結構異常 | `ERROR` | 跳過此檔，中止流程 |
| 來源資料非數值 | `ERROR` | 跳過此檔，中止流程 |
| 公司代號不在模板中 | `ERROR` | 跳過此公司 |
| 同公司同季度重複 | `ERROR` | 中止整個流程 |
| 年度不一致 | `ERROR` | 中止整個流程 |
| 模板/Sheet 不存在 | `ERROR` | 中止整個流程 |
| 寫入/輸出失敗 | `ERROR` | 中止整個流程 |
| 去年檔案不存在 | `WARN` | U 欄留空，繼續 |
| 驗算差異 (F≠C+D-E) | `WARN` | 記錄但繼續 |
| 正常處理步驟 | `INFO` | 記錄進度 |

### 13.3 日誌輸出範例

```
2026-04-23 17:00:01 [INFO] ReportGeneratorRunner - ===== 自留保費統計表報表轉換系統 =====
2026-04-23 17:00:01 [INFO] ReportGeneratorService - 掃描輸入目錄: ./input
2026-04-23 17:00:01 [INFO] ReportGeneratorService - 找到 3 個 .xlsx 檔案
2026-04-23 17:00:01 [INFO] ReportGeneratorService - 解析檔名...
2026-04-23 17:00:01 [INFO] ReportGeneratorService -   已解析: 29_115(01-03)_自留保費統計表.xlsx → 公司=29, 年度=115, Q1
2026-04-23 17:00:01 [INFO] ReportGeneratorService - 前置驗證...
2026-04-23 17:00:02 [INFO] ReportGeneratorService - 讀取來源檔案...
2026-04-23 17:00:02 [INFO] ExcelReaderService - Successfully read source file: ... company=29 (美國國際), entries=33
2026-04-23 17:00:02 [INFO] ReportGeneratorService - 按季度分組...
2026-04-23 17:00:02 [INFO] ReportGeneratorService - 年度=115, 最大季度=Q1, 涵蓋季度=[1]
2026-04-23 17:00:02 [WARN] LastYearReaderService - 去年報表不存在: 114年產險業務(Q1季自留)保費統計表.xlsx, U欄將留空
2026-04-23 17:00:03 [INFO] ReportGeneratorService - 寫入報表: output/115年產險業務(Q1季自留)保費統計表.xlsx
2026-04-23 17:00:03 [INFO] TemplateWriterService - Report written successfully to output/...
2026-04-23 17:00:03 [INFO] ReportGeneratorService - ===== 處理完成 =====
```

---

## 14. 常見問題 (FAQ)

### Q: 程式執行後沒有產出檔案？

檢查 log 輸出。常見原因：
1. `input/` 目錄中沒有 `.xlsx` 檔案
2. 檔名格式不符規定
3. `templates/template.xlsx` 不存在
4. 來源資料有誤導致中止

### Q: 輸出報表的公式沒有計算結果？

這是正常的。程式設定了 `setForceFormulaRecalculation(true)`，用 Excel 開啟檔案時會自動重新計算所有公式。

### Q: 可以同時處理不同年度嗎？

不可以。系統會驗證所有來源檔的年度必須一致，不同年度會中止處理。

### Q: 新增一家公司（不在模板的 19 家中）怎麼辦？

目前需修改模板 Excel，在各季度區塊中新增該公司的列。程式會自動讀取模板中的公司代號進行匹配。

### Q: Docker 環境如何看 log？

```bash
docker-compose logs -f report-transformer
```

### Q: 如何修改日誌等級？

方式一：修改 `application.yml`
```yaml
logging:
  level:
    com.example.retainedpremium: DEBUG
```

方式二：透過環境變數
```bash
LOGGING_LEVEL_COM_EXAMPLE_RETAINEDPREMIUM=DEBUG
```
